export type Clip = {
  id: string;
  title: string;
  tags: string;
  thumbnailUrl: string;
  fileUrl: string;
  fileName: string;
  uploadDate: string; // ISO string
  userId: string;
  featured: boolean;
};
